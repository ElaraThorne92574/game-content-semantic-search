{
  "name": "game-content-semantic-search",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "seed": "npm run build && node dist/src/seed_game_content.js",
    "start": "npm run build && node dist/src/search_server.js",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "openai": "^5.12.2",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^22.17.2",
    "typescript": "^5.9.2"
  }
}
